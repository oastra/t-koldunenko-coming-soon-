const SectionTitle = ({ children }) => {
  return (
    <h2 className="text-2xl md:text-3xl font-display font-semibold text-mainBlue mb-8">
      {children}
    </h2>
  );
};

export default SectionTitle;
